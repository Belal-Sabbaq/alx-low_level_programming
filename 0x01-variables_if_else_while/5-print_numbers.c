#include <stdio.h>

/**
 * main - Entry point
 * Description: 'di'
 * Return: Always 0 (Success)
 */
int main(void)
{
char a;
/* your code goes there */
for (a = '0' ; a < '9' + 1  ; a++)
{
	putchar(a);
}
putchar('\n');
return (0);
}
