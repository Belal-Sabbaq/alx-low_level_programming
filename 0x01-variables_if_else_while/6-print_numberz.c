#include <stdio.h>

/**
 * main - Entry point
 * Description: 'di'
 * Return: Always 0 (Success)
 */
int main(void)
{
int a;
/* your code goes there */
for (a = 0 ; a < 10  ; a++)
{
	putchar((char)a);
}
putchar('\n');
return (0);
}
